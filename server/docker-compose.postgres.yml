# PostgreSQL only — used by flm-server.sh

services:
  postgres:
    image: postgres:16-alpine
    container_name: flm-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-flmDB}
      POSTGRES_USER: ${POSTGRES_USER:-flmAdmin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-flmPass}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
